# jplearn 项目 Agent

## 角色定位
这是一个面向 jplearn 的项目级 Agent，负责理解、维护和扩展这个“日语歌词学习”项目。它适合在需要改前端界面、调整数据结构、修复 Worker 流程、补充文档或整理 TODO 时使用。

## 何时使用这个 Agent
当你要处理以下任务时，优先使用这个 Agent：
- 修改前端页面、路由、交互逻辑或样式
- 调整歌曲/解析的数据结构或 JSON 格式
- 维护 Cloudflare Worker 的抓取、解析和 GitHub 写入流程
- 排查 AI 解析、导入歌词、保存数据等流程问题
- 为项目补充说明文档、验收清单或后续开发计划

## 项目背景
jplearn 是一个“日语歌词学习”应用，当前实现方式是：
- 前端为静态页面，直接部署在 GitHub Pages
- 关键写操作通过 Cloudflare Worker 代理到 GitHub 仓库
- 歌曲与解析结果以 JSON 文件形式保存在 data/ 目录下

这套架构的目标是让项目尽量轻量、易部署、便于后续扩展。

## 核心模块与现状

### 1. 前端入口
- [index.html](index.html)：唯一入口页面，加载前端脚本。
- [assets/app.js](assets/app.js)：负责整个 SPA 的路由、首页、歌词详情、导入流程、AI 解析触发与本地配置。
- [assets/style.css](assets/style.css)：整体视觉风格与组件样式。

当前状态：
- 首页搜索、歌曲列表、歌词详情页、单词点击、句子查看等基础体验已经实现。
- 未解析歌曲会显示引导页，用户可配置自己的 AI API 并开始解析。

### 2. 数据层
- [data/index.json](data/index.json)：歌曲索引，记录歌曲列表与解析数量。
- [data/songs](data/songs)：每首歌曲的基本信息和原始歌词。
- [data/analysis](data/analysis)：每个解析版本的结构化结果。

当前状态：
- 目前已有一条示例歌曲和一条示例解析版本。
- 数据结构已经支持多个 analysis_versions，但详情页目前还没有做版本切换器。

### 3. Worker 层
- [worker/worker.js](worker/worker.js)：负责三类后端能力：
  - Utaten 歌词导入代理
  - AI 解析转发并保存结果
  - 手动创建歌曲记录
- [worker/wrangler.toml](worker/wrangler.toml)：Cloudflare Worker 部署配置。

当前状态：
- 代码已实现主流程骨架。
- 但真正部署后才能使用写入能力。
- Utaten 抓取逻辑目前是占位实现，实际网页结构需要进一步适配。

## 重要约束
- 优先保持项目“轻量、静态、可直接部署”的特性，避免引入复杂构建系统。
- 修改数据结构时要兼容现有 JSON 结构，避免破坏已有歌曲和解析记录。
- 前端 API 配置（地址 / Key / 模型）应继续使用浏览器 localStorage，而不是把秘密写进仓库。
- Worker 相关改动要注意 GitHub Token、Owner、Repo、Branch 等配置的安全性。
- 新增字段时，尽量考虑向后兼容，并同步更新样例数据与说明文档。

## 工作原则
1. 先理解上下文，再动手改代码。
2. 优先做最小改动，避免引入不必要的依赖。
3. 修改前端时，保持现有路由和页面结构可用。
4. 修改数据结构时，确保 index.json、song 文件和 analysis 文件的语义一致。
5. 发现 TODO 或不完整实现时，尽量把现状和风险写清楚，而不是假装已完成。

## 常见任务指导

### 如果任务是前端相关
- 先查看 [assets/app.js](assets/app.js) 和 [assets/style.css](assets/style.css)
- 关注页面路由、数据渲染和用户交互流
- 保持“静态页面 + hash 路由”的实现方式不变

### 如果任务是数据相关
- 先看 [data/index.json](data/index.json)、[data/songs](data/songs) 和 [data/analysis](data/analysis)
- 明确新增字段是否会影响前端渲染或 Worker 逻辑
- 对样例数据进行同步更新

### 如果任务是 Worker 相关
- 先看 [worker/worker.js](worker/worker.js)
- 关注 API 入口、GitHub 写入逻辑、AI 输出解析逻辑
- 保证错误信息清晰，便于排查部署或接口问题

## 交付要求
每次完成修改后，建议输出以下内容：
- 影响了哪些模块
- 改动了哪些文件
- 是否涉及数据结构变更
- 是否需要补充部署或配置说明
- 目前的验证情况

## 项目当前待办重点
- Utaten 抓取逻辑仍需根据真实页面结构进一步修正
- 解析版本切换 UI 尚未实现
- 仍缺少更完整的歌曲/解析后台管理界面

这个 Agent 的目标不是“把项目改成一个复杂框架应用”，而是持续保持它在“轻量、可学习、可扩展”的方向上稳定演进。